# Goal: Deliver Lurkline v0.8.1 Audit Remediation

Work in
`/Users/smarzola/.codex/worktrees/efb7b9a5-eb04-470b-9f50-3fb07f172a72/lurkline`.

Remediate every confirmed finding from the 2026-07-29 full and adversarial
audit, preserve Lurkline's fail-closed Slack mutation model, and publish a
verified v0.8.1 patch release. The implementation must make uncertain draft
outcomes identifiable, eliminate destructive file-ownership races, bound the
complete inbox result, remove the identified safety-critical duplication, and
make release publication depend on the repository's complete quality gates.

Source of truth: the user's request in the current Codex task and the audit
findings immediately preceding it.

## Target State

When this goal is complete:

- Deleting a one-file draft cannot delete a Slack file that another process may
  have attached after a client-side ownership scan.
- Text draft creation accepts only an exact correlated acknowledgement or an
  exact bounded reconciliation result; unresolved outcomes retain a safe
  client message ID and explicitly prohibit blind retry.
- Ambiguous text draft deletion reconciles exact absence or reports a
  structured uncertain outcome.
- A complete inbox result has one operation-wide byte limit and explicit
  truncation metadata.
- Upload preparation and mutation-ambiguity classification have one shared
  implementation used by every affected workflow.
- CI runs the credential scanner, declared Rust 1.88 compatibility is checked,
  macOS-specific behavior is tested on macOS, and the exact release SHA cannot
  publish unless those gates plus formatting, lint, locked tests, release
  build, and packaging succeed.
- `main` contains the remediations and v0.8.1 metadata, and annotated tag
  `v0.8.1` has a successful GitHub release with verified Linux x86-64, Linux
  ARM64, and macOS ARM64 archives and matching checksums.

## Current-State Evidence

Verified before implementation:

- Starting branch: `fix/remediate-audit-findings`.
- Starting and goal base commit:
  `dedff55e9f3107ce29e1de53d05c21c56c28752e`, identical to `origin/main`.
- `src/service.rs::prove_exclusive_file_draft` completes its client-side scan
  before `delete_draft` later calls `drafts.delete` with
  `skip_file_deletion=false`, leaving a cross-process TOCTOU file-deletion
  window.
- `src/service.rs::create_draft` generates its UUID inline, accepts a returned
  draft without checking that ID or exact blocks, and propagates ambiguous
  transport outcomes without reconciliation.
- `src/service.rs::delete_draft` reconciles ambiguous one-file deletion but
  directly propagates ambiguous text-only deletion.
- `src/service.rs::inbox` can retain 50 independently response-bounded history
  pages without an aggregate byte budget.
- `src/service.rs` duplicates upload allocation/transfer preparation between
  direct uploads and one-file drafts and defines three identical mutation
  ambiguity predicates.
- `.github/workflows/ci.yml` omits the credential scanner, Rust 1.88, and macOS
  tests. `.github/workflows/release.yml` publishes after tag validation and
  builds without depending on the full CI workflow.
- The clean baseline passed formatting, strict locked Clippy, all 220 tests,
  locked release build, credential scan, deterministic package/checksum smoke,
  and a RustSec scan of 233 locked dependencies against 1,172 advisories.
- Latest release and package version: v0.8.0.

Unknowns affecting implementation details but not the target state:

- Whether Slack always echoes `client_msg_id` for text draft creation.
  Fail closed and reconcile against the complete bounded active-draft snapshot;
  do not weaken correlation if it does not.

## Constraints And Authorization

Follow `AGENTS.md`.

- Keep CLI and MCP behavior backed by the same typed service layer.
- Preserve existing read behavior, write gates, confirmations, stored
  credential behavior, private endpoint compatibility, and structured output
  compatibility except for truthful additive fields or new explicit uncertain
  errors required by this goal.
- Never commit, log, snapshot, or print real Slack credentials, cookies, HAR
  payloads, workspace messages, user data, or live identifiers. Use synthetic
  fixtures for tests and examples.
- For one-file draft deletion, prefer preservation over deletion. No sequence
  of client-side reads can atomically prove file exclusivity at the later Slack
  mutation, so v0.8.1 must send `skip_file_deletion=true` and report the file as
  preserved.
- Bound reconciliation attempts, active-draft scans, serialized inbox output,
  and all newly accepted fields. Never automatically retry an uncertain Slack
  mutation.
- Implement the smallest coherent complete design. Reuse established types and
  helpers; do not add dependencies, background cleanup, a provenance registry,
  general retry framework, or unrelated Slack capability.
- Preserve unrelated user changes. The baseline is clean.

This goal authorizes in-scope inspection, local edits, Conventional Commit
checkpoints, non-destructive verification, branch pushes, a pull request,
merge after green exact-head CI and review, annotated tag `v0.8.1`, GitHub
release publication, and release asset/checksum verification.

The user also authorizes credential-safe live validation through their existing
Slack browser session in workspace `sferait-ws`, exclusively as user
`smarzola` sending synthetic DMs to `smarzola`. Use live Slack only when useful
to validate the changed contract. Do not inspect or mutate existing content;
keep credentials ephemeral and live output out of tracked files and review
packets.

Ask only when an ambiguity would materially change user-visible behavior,
architecture, data compatibility, security posture, or authorization.
Otherwise follow repository evidence and record material decisions here.
Before declaring a blocker, exhaust safe in-scope alternatives and report the
evidence without claiming completion.

## Success Criteria

The goal is complete only when:

1. One-file draft deletion always preserves the file and tests prove no
   cross-process timing can request destructive file deletion.
2. Text draft creation validates the exact client ID, destination, empty file
   set, and authored blocks; ambiguous or mismatched responses reconcile by
   exact bounded reads or return a structured creation-uncertain error carrying
   the client ID.
3. Text-only draft deletion reconciles ambiguous outcomes and otherwise returns
   `DraftMutationUncertain`.
4. Inbox accumulation stops before its serialized result exceeds the configured
   response-byte limit and reports why it truncated.
5. Direct file upload and one-file draft creation share one allocation and
   transfer preparation state machine, and every affected Slack mutation uses
   one ambiguity predicate.
6. Synthetic unit, CLI, MCP, and workflow coverage proves the corrected
   behavior without real Slack data.
7. CI scans secrets, tests macOS, checks Rust 1.88, and the release workflow
   invokes the exact-SHA reusable quality workflow before native builds or
   publication.
8. v0.8.1 version metadata and operator documentation are consistent with the
   implemented behavior.
9. Every milestone has passing verification, a clean retained-reviewer result,
   a checked status note, and a focused Conventional Commit.
10. Final local verification and a fresh context-independent audit pass after
    the final repair.
11. The pull request merges only after exact-head CI passes; annotated tag
    `v0.8.1`, its release workflow, release object, six assets, checksums,
    archive layouts, architectures, and packaged runtime version all verify.

## Milestones

- [x] Milestone 1: Make every draft mutation outcome safe and identifiable.
- [x] Milestone 2: Bound inbox output and consolidate safety-critical service
  machinery.
- [x] Milestone 3: Enforce delivery gates and prepare v0.8.1.

### Checkpoint Protocol

For each milestone:

1. Meet its acceptance criteria and run the listed verification.
2. Freeze writes and obtain a clean review from the retained adversarial
   reviewer; repair and re-review until no blocking finding remains.
3. Mark the milestone `[x]` and add a dated status note with exact commands and
   results.
4. Commit implementation, tests, documentation, and this goal update together
   with a focused Conventional Commit.
5. Report the resulting hash before starting the next milestone.

Do not mark or commit a failed milestone. Diagnose and repair in-scope failures
instead of weakening tests.

## Milestone 1: Safe Draft Mutation Outcomes

Acceptance criteria:

- File-draft deletion preserves its exact file and truthfully reports that
  preservation after acknowledged or reconciled draft absence.
- Text creation proves exact acknowledgement or exact reconciliation and
  exposes a secret-safe structured uncertain result with a stable client ID.
- Text deletion reconciles ambiguous absence and uses the existing structured
  mutation-uncertain boundary.
- CLI and MCP render the new uncertain outcome consistently through the shared
  service error.

Likely touchpoints: `src/service.rs`, `src/error.rs`, `src/mcp.rs`,
`src/model.rs`, `src/cli.rs`, and synthetic tests.

Verification:

```bash
cargo test --locked service::tests::draft
cargo test --locked mcp::tests
cargo test --locked --test cli_process
cargo clippy --locked --all-targets -- -D warnings
```

Status: Complete (2026-07-29). One-file deletion now always preserves its
file; text creation requires exact acknowledgement or bounded exact
reconciliation and otherwise returns the sent client ID in a structured
creation-uncertain error; ambiguous text deletion proves absence or reports an
uncertain mutation. Verification passed `cargo fmt --all -- --check`,
`cargo test --locked --lib` outside the restrictive sandbox (210 passed),
`cargo test --locked mcp::tests` (5 passed),
`cargo test --locked --test cli_process` (12 passed), and
`cargo clippy --locked --all-targets -- -D warnings`. The retained reviewer
requested isolated correlation-predicate coverage; the repair added independent
client-ID, route, file-set, authored-block, and duplicate-match cases with
stable-ID assertions. Re-review reported no findings.

## Milestone 2: Bounded And Shared Service Machinery

Acceptance criteria:

- Inbox results use an operation-wide serialized byte budget derived from the
  configured response limit, stop issuing history requests once exhausted, and
  expose deterministic truncation metadata in typed CLI/MCP output.
- Upload allocation, signed-URL validation, source stability, transfer, and
  post-transfer source verification are implemented once and mapped truthfully
  into the existing upload and file-draft reports.
- HTTP/transport/invalid-response mutation ambiguity is classified once without
  changing existing safe reaction, publication, upload, or draft behavior.

Likely touchpoints: `src/service.rs`, `src/model.rs`, `src/cli.rs`, `src/mcp.rs`,
and synthetic tests.

Verification:

```bash
cargo test --locked service::tests::inbox
cargo test --locked service::tests::upload
cargo test --locked --test mcp_raw_stdio
cargo clippy --locked --all-targets -- -D warnings
```

Status: Complete (2026-07-29). Inbox reports now use
`LURKLINE_MAX_RESPONSE_BYTES` as one operation-wide budget against their
pretty-serialized representation, stop after the first non-fitting history
candidate, and report deterministic `conversation_limit` or `byte_limit`
truncation through typed CLI/MCP output. Direct uploads and one-file draft
creation share one allocation, signed-URL, source-stability, transfer, and
post-transfer verification state machine. Publication, reaction, upload, and
draft mutations use one ambiguity predicate. Verification passed
`cargo test --locked --lib` outside the restrictive sandbox (213 passed),
`cargo test --locked --test mcp_raw_stdio` (2 passed),
`cargo test --locked --test cli_process` (12 passed),
`cargo clippy --locked --all-targets -- -D warnings`, formatting/diff checks,
and the credential scanner. The retained reviewer found a false human
“Inbox is clear” message for an empty byte-truncated result and missing MCP
schema proof; the repair corrected the clear-state predicate, added exact CLI
formatting coverage, and asserted both truncation enum values in the raw MCP
schema. Re-review reported no findings.

## Milestone 3: Enforced Delivery Gates And v0.8.1

Acceptance criteria:

- CI is reusable by the release workflow and contains formatting, strict
  Clippy, locked tests, release build, package smoke, credential scan, macOS
  tests, and Rust 1.88 compatibility coverage.
- Release build and publication cannot start until that exact tagged SHA passes
  the reusable workflow.
- Package metadata and README consistently report v0.8.1 and document preserved
  file deletion, uncertain draft creation, inbox byte truncation, and current
  verification commands.
- Local package generation is deterministic and passes checksum, layout,
  architecture, executable-mode, and version readback checks.

Likely touchpoints: `.github/workflows/ci.yml`,
`.github/workflows/release.yml`, `Cargo.toml`, `Cargo.lock`, `README.md`, and
packaging tests.

Verification:

```bash
cargo fmt --all -- --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked --all-targets
cargo build --release --locked
target/release/lurkline --version
python3 scripts/check-no-secrets.py
git ls-files '*.har'
git diff --check
```

Status: Complete (2026-07-29). CI is now reusable and runs formatting, strict
locked Clippy, all-target locked tests, a locked release build, deterministic
package smoke, the credential scanner, a Rust 1.88 all-target check, and
macOS ARM64 all-target tests. The tag workflow validates first, calls that
workflow from the same tagged commit, and blocks native builds and publication
on its success. Cargo, the lockfile, packaged MCP metadata, runtime-derived MCP
metadata, README examples, and archive names report v0.8.1. The full local gate
passed 213 library tests, 12 CLI process tests, 2 raw MCP tests, and 1 package
metadata test, plus formatting, strict Clippy, locked release build/runtime
`lurkline 0.8.1`, credential and diff checks. Rust 1.88 checked every target.
RustSec scanned 233 locked dependencies against 1,173 advisories with no
finding. Two clean-directory macOS packages were byte-identical; checksum
`dc125fe270d157f98c16dc64de6b2da702e6006f2da65dc0df7303363b0ee5e7`
verified, with the exact binary/README/LICENSE layout, executable mode, Mach-O
ARM64 architecture, and packaged runtime `lurkline 0.8.1`. Version-gate tests
first caught stale MCP runtime and package metadata; both duplicates were
removed or synchronized before the passing run. Workflow YAML parsing and
direct reusable-workflow review passed; execution policy rejected downloading
`actionlint`. The retained reviewer reported no findings.

## Final Verification

Run from the repository root:

```bash
cargo fmt --all -- --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked --all-targets
cargo build --release --locked
target/release/lurkline --version
python3 scripts/check-no-secrets.py
git ls-files '*.har'
cargo audit --file Cargo.lock
git diff --check
git status --short
```

Build v0.8.1 twice in separate temporary directories with
`scripts/package-release.py`; require identical archives and checksums. Inspect
the macOS archive for its exact three-file layout, executable bit, Mach-O ARM64
binary, and `lurkline 0.8.1` runtime readback.

After final verification, obtain a clean fresh context-independent audit.
Then push the branch, open the pull request, verify exact-head CI, merge, verify
updated `main`, create and push annotated tag `v0.8.1`, and verify the release
workflow, release object, six assets, checksums, archive layouts,
architectures, executable bits, and packaged runtime version.

## Resume And Final Report

On resume, read this goal, `AGENTS.md`, git status, milestone notes, recent
commits, PR state, tags, and releases. Continue from the first unchecked
milestone without redoing verified checkpoints.

Lead the final report with `Achieved` or `Not achieved` and include the goal
file, branch, checkpoint commits, important files changed, exact local/CI/live
Slack/audit/release verification, reviewer rounds and dispositions, and any
residual private-API risks or blockers.
